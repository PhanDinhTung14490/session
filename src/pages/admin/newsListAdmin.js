/* eslint-disable linebreak-style */

import NewsList from "../../components/admin/newsList";

const ListAdmin = {
    runder() {
        return `
        <div> ${NewsList.runder()} </div>
        `;
    },
};

export default ListAdmin;
